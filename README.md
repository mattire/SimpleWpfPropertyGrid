Usage:

            var window = new PropertyGridWindow(yourObject, "test")
            {
                Owner = parentWindow
            };
            window.Show();

This repository contains .Net 9 libraries for .Net 8 checkout SimpleWpfPropertyGridNet8 repository.
